//create a user array that can have a string or number as values
// this means that the array can contain either, there is no restriction on order
var user = [1, "hc"];
var userAPI = ["LAD", 12, true];
console.log(userAPI);
console.log(userAPI[0]);
